# Examples

Deployment and instance configuration templates:

- [`config.py`](config.py) — place in the Flask instance folder
- [`webpass.ini.example`](webpass.ini.example) — uWSGI configuration
- [`webpass.service.example`](webpass.service.example) — systemd unit
